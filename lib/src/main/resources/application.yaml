micronaut:
  application:
    name: avatar-animation-server
  server:
    port: 8080
  router:
    static-resources:
      default:
        enabled: true
        mapping: /**
        paths: classpath:public